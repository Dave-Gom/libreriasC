#include <stdio.h>
#include <time.h>
#include <math.h>
#include <stdlib.h>

void Imprime( int i, int j, int a[][j]);
void imprimeCamino( int i, int j, int a[][j]);
int verifica(int i, int j, int a[][j]);
char getLetra(int val);